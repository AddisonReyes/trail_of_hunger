[package]
name = "trail_of_hunger"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = { version = "0.4.14", features = ["audio"] }

[patch.crates-io]
miniquad = { git = "https://github.com/not-fl3/miniquad", branch = "master" }
